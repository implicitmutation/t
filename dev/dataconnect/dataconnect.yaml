specVersion: "v1alpha"
serviceId: "local"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "dataconnect"
      cloudSql:
        instanceId: "my-instance"
connectorDirs: ["./connector"]